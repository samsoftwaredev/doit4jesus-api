{
  "info": {
    "name": "Holy Competition API",
    "_postman_id": "holy-competition-api-v1",
    "description": "REST API for the Holy Competition app. Built with Next.js Route Handlers and Supabase Auth.\n\n**Authentication**\nAll endpoints (except `/health`) require a valid Supabase JWT.\nSet the `accessToken` collection variable to your Supabase `access_token` and every request will include it automatically as a Bearer token.\n\n**Idempotency**\nEvery reward-bearing POST requires an `Idempotency-Key`. Keep the same key when retrying a request; use a new key for a new action.\n\n**Base URL**\nSwitch the `baseUrl` variable between `http://localhost:3000` (local) and your production URL.",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "variable": [
    {
      "key": "baseUrl",
      "value": "http://localhost:3000",
      "description": "Base URL of the API. Change to your production URL when needed."
    },
    {
      "key": "accessToken",
      "value": "",
      "description": "Supabase access_token (JWT). Paste the value returned by supabase.auth.getSession()."
    },
    {
      "key": "notificationId",
      "value": "00000000-0000-0000-0000-000000000000",
      "description": "UUID of a notification to mark as read."
    },
    {
      "key": "assignmentId",
      "value": "00000000-0000-0000-0000-000000000000",
      "description": "UUID of a challenge assignment to claim."
    },
    {
      "key": "demonCode",
      "value": "SCROLLZILLA",
      "description": "Code of the demon to inspect or encounter."
    },
    {
      "key": "encounterId",
      "value": "00000000-0000-0000-0000-000000000000",
      "description": "UUID of a demon encounter. Set this from a start or list-encounters response."
    },
    {
      "key": "defenseAssignmentId",
      "value": "00000000-0000-0000-0000-000000000000",
      "description": "UUID of a defense assignment within an encounter."
    },
    {
      "key": "churchId",
      "value": "c6000000-0000-4000-8000-000000000001",
      "description": "UUID of a church. The default is the seeded St. Mary Cathedral record."
    },
    {
      "key": "churchChangeRequestId",
      "value": "c8000000-0000-4000-8000-000000000001",
      "description": "UUID of a church change request. The default is the seeded pending request."
    },
    {
      "key": "friendId",
      "value": "11111111-1111-4111-8111-111111111111",
      "description": "UUID of a confirmed friend. The default is the seeded Maria Santos friendship."
    },
    {
      "key": "friendRequestId",
      "value": "fb000000-0000-4000-8000-000000000002",
      "description": "UUID of a friend request. The default is John Paul's seeded pending request to Test User."
    },
    {
      "key": "outgoingFriendRequestId",
      "value": "00000000-0000-0000-0000-000000000000",
      "description": "Set this to one of the authenticated user's pending outgoing friend requests before using the cancel request."
    }
  ],
  "auth": {
    "type": "bearer",
    "bearer": [
      {
        "key": "token",
        "value": "{{accessToken}}",
        "type": "string"
      }
    ]
  },
  "item": [
    {
      "name": "Health",
      "description": "Service liveness check — no authentication required.",
      "item": [
        {
          "name": "Service health",
          "request": {
            "auth": { "type": "noauth" },
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/health",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "health"]
            },
            "description": "Returns 200 when the API is reachable. No auth token required. Use this as a liveness probe."
          }
        }
      ]
    },
    {
      "name": "Daily Scripture",
      "description": "The current liturgical reading and its available Scripture text.",
      "item": [
        {
          "name": "Get daily Scripture",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/scripture",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "scripture"],
              "query": [
                {
                  "key": "date",
                  "value": "2026-07-25",
                  "description": "Optional calendar date in YYYY-MM-DD format. Omit to retrieve today's reading in the America/Chicago calendar.",
                  "disabled": true
                }
              ]
            },
            "description": "Returns the day's celebration, lectionary number, Scripture references, and any enriched Scripture text. The collection's Bearer token authentication applies automatically.\n\nUse the optional `date` query parameter to retrieve a specific day. Returns **404** when no reading has been imported for the requested date."
          }
        }
      ]
    },
    {
      "name": "Churches",
      "description": "Search the approved church directory and retrieve a church's recurring services with its live status.",
      "item": [
        {
          "name": "Search churches",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/churches?countryCode=US&city=Austin&diocese=Diocese%20of%20Austin&limit=20&offset=0",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "churches"],
              "query": [
                {
                  "key": "countryCode",
                  "value": "US",
                  "description": "Optional two-letter country code. If multiple filters are supplied, each must match the same church."
                },
                {
                  "key": "city",
                  "value": "Austin",
                  "description": "Optional church-city name. This is not an app.cities ID. If multiple filters are supplied, each must match the same church."
                },
                {
                  "key": "diocese",
                  "value": "Diocese of Austin",
                  "description": "Optional canonical diocese name. If multiple filters are supplied, each must match the same church."
                },
                {
                  "key": "limit",
                  "value": "20",
                  "description": "Results per page (1–100)."
                },
                {
                  "key": "offset",
                  "value": "0",
                  "description": "Zero-based page offset."
                }
              ]
            },
            "description": "Returns approved churches. Supply at least one country, city, or diocese filter; when multiple filters are supplied, they must all match the same church. Results include latitude and longitude; retrieve one church for its full address, weekly schedule, and live availability."
          }
        },
        {
          "name": "Get church details and live status",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/churches/{{churchId}}",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "churches", "{{churchId}}"]
            },
            "description": "Returns the complete church address, latitude/longitude, diocese, recurring weekday Mass/confession/adoration times, and live status. `massInProgress`, `confessionInProgress`, and `adorationInProgress` are computed at request time in the church's timezone and may all be true together. A missing end time means one hour."
          }
        }
      ]
    },
    {
      "name": "My Churches",
      "description": "Link churches to the current user and optionally set one as primary.",
      "item": [
        {
          "name": "List my linked churches",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/me/churches",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "me", "churches"]
            },
            "description": "Lists the authenticated user's church links, primary flag, and church summaries."
          }
        },
        {
          "name": "Link a church",
          "request": {
            "method": "POST",
            "header": [{ "key": "Content-Type", "value": "application/json" }],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"churchId\": \"{{churchId}}\",\n  \"isPrimary\": true\n}",
              "options": { "raw": { "language": "json" } }
            },
            "url": {
              "raw": "{{baseUrl}}/api/v1/me/churches",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "me", "churches"]
            },
            "description": "Links an active church to the authenticated user. Set `isPrimary` to true to atomically make it the user's only primary church."
          }
        },
        {
          "name": "Set primary church",
          "request": {
            "method": "PATCH",
            "header": [{ "key": "Content-Type", "value": "application/json" }],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"isPrimary\": true\n}",
              "options": { "raw": { "language": "json" } }
            },
            "url": {
              "raw": "{{baseUrl}}/api/v1/me/churches/{{churchId}}",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "me", "churches", "{{churchId}}"]
            },
            "description": "Makes an already-linked church the user's primary church."
          }
        },
        {
          "name": "Unlink a church",
          "request": {
            "method": "DELETE",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/me/churches/{{churchId}}",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "me", "churches", "{{churchId}}"]
            },
            "description": "Removes a church link for the authenticated user. Returns 204."
          }
        }
      ]
    },
    {
      "name": "Church Change Requests",
      "description": "Submit a proposed church or full replacement weekly schedule for moderator approval.",
      "item": [
        {
          "name": "List my church change requests",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/church-change-requests?status=all&limit=20&offset=0",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "church-change-requests"],
              "query": [
                {
                  "key": "status",
                  "value": "all",
                  "description": "pending | approved | rejected | all"
                },
                {
                  "key": "limit",
                  "value": "20",
                  "description": "Results per page (1–100)."
                },
                {
                  "key": "offset",
                  "value": "0",
                  "description": "Zero-based page offset."
                }
              ]
            },
            "description": "Lists change requests submitted by the authenticated user."
          }
        },
        {
          "name": "Submit a schedule replacement",
          "request": {
            "method": "POST",
            "header": [{ "key": "Content-Type", "value": "application/json" }],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"requestType\": \"scheduleUpdate\",\n  \"churchId\": \"{{churchId}}\",\n  \"serviceTimes\": [\n    {\n      \"serviceType\": \"mass\",\n      \"weekday\": \"sunday\",\n      \"startTime\": \"08:00\",\n      \"endTime\": null\n    },\n    {\n      \"serviceType\": \"confession\",\n      \"weekday\": \"saturday\",\n      \"startTime\": \"16:00\",\n      \"endTime\": \"17:00\"\n    }\n  ],\n  \"notes\": \"These times replace the church's current weekly schedule after approval.\"\n}",
              "options": { "raw": { "language": "json" } }
            },
            "url": {
              "raw": "{{baseUrl}}/api/v1/church-change-requests",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "church-change-requests"]
            },
            "description": "Creates a pending change request. On approval, the supplied service-time array replaces the church's entire recurring weekly schedule. Omit `endTime` to use the one-hour default. Use `requestType: createChurch` with a `church` object, including required numeric `latitude` and `longitude`, to propose a new church."
          }
        },
        {
          "name": "Submit a new church",
          "request": {
            "method": "POST",
            "header": [{ "key": "Content-Type", "value": "application/json" }],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"requestType\": \"createChurch\",\n  \"church\": {\n    \"dioceseId\": \"c5000000-0000-4000-8000-000000000001\",\n    \"name\": \"Example Catholic Church\",\n    \"addressLine1\": \"123 Example Street\",\n    \"addressLine2\": null,\n    \"city\": \"Austin\",\n    \"regionName\": \"Texas\",\n    \"postalCode\": \"78701\",\n    \"countryCode\": \"US\",\n    \"timezone\": \"America/Chicago\",\n    \"latitude\": 30.2672,\n    \"longitude\": -97.7431\n  },\n  \"serviceTimes\": [\n    {\n      \"serviceType\": \"mass\",\n      \"weekday\": \"sunday\",\n      \"startTime\": \"09:00\",\n      \"endTime\": null\n    }\n  ],\n  \"notes\": \"Please add this church to the directory.\"\n}",
              "options": { "raw": { "language": "json" } }
            },
            "url": {
              "raw": "{{baseUrl}}/api/v1/church-change-requests",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "church-change-requests"]
            },
            "description": "Creates a pending church-directory request. Latitude (-90 to 90) and longitude (-180 to 180) are required and stored when an administrator approves the request."
          }
        }
      ]
    },
    {
      "name": "Admin · Church Change Requests",
      "description": "Administrator-only review queue. test@test.com is the seeded admin account.",
      "item": [
        {
          "name": "List church change requests",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/admin/church-change-requests?status=pending&limit=20&offset=0",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "admin", "church-change-requests"],
              "query": [
                {
                  "key": "status",
                  "value": "pending",
                  "description": "pending | approved | rejected | all"
                },
                {
                  "key": "limit",
                  "value": "20",
                  "description": "Results per page (1–100)."
                },
                {
                  "key": "offset",
                  "value": "0",
                  "description": "Zero-based page offset."
                }
              ]
            },
            "description": "Lists submitted church change requests. Requires the authenticated user to hold the app admin role."
          }
        },
        {
          "name": "Approve a church change request",
          "request": {
            "method": "PATCH",
            "header": [{ "key": "Content-Type", "value": "application/json" }],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"decision\": \"approved\"\n}",
              "options": { "raw": { "language": "json" } }
            },
            "url": {
              "raw": "{{baseUrl}}/api/v1/admin/church-change-requests/{{churchChangeRequestId}}",
              "host": ["{{baseUrl}}"],
              "path": [
                "api",
                "v1",
                "admin",
                "church-change-requests",
                "{{churchChangeRequestId}}"
              ]
            },
            "description": "Approves a pending request. A new-church request creates the church and its schedule; a schedule-update request atomically replaces the existing church schedule and awards the submitting user a repeatable Church Schedule Steward badge."
          }
        }
      ]
    },
    {
      "name": "Friends",
      "description": "Username-only friend requests, confirmed-friend stats, earned badges, and period-points rankings. Usernames are matched case-insensitively.",
      "item": [
        {
          "name": "List incoming friend requests",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/me/friend-requests?direction=incoming&status=pending&limit=20&offset=0",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "me", "friend-requests"],
              "query": [
                {
                  "key": "direction",
                  "value": "incoming",
                  "description": "incoming | outgoing | all"
                },
                {
                  "key": "status",
                  "value": "pending",
                  "description": "pending | accepted | rejected | cancelled | all"
                },
                { "key": "limit", "value": "20" },
                { "key": "offset", "value": "0" }
              ]
            },
            "description": "Lists requests involving the authenticated user. Defaults are incoming pending requests."
          }
        },
        {
          "name": "Send friend request by username",
          "request": {
            "method": "POST",
            "header": [{ "key": "Content-Type", "value": "application/json" }],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"username\": \"johnpaul\"\n}",
              "options": { "raw": { "language": "json" } }
            },
            "url": {
              "raw": "{{baseUrl}}/api/v1/me/friend-requests",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "me", "friend-requests"]
            },
            "description": "Creates a pending request and recipient notification. A reciprocal pending request is accepted automatically; matching is case-insensitive."
          }
        },
        {
          "name": "Accept friend request",
          "request": {
            "method": "PATCH",
            "header": [{ "key": "Content-Type", "value": "application/json" }],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"decision\": \"accepted\"\n}",
              "options": { "raw": { "language": "json" } }
            },
            "url": {
              "raw": "{{baseUrl}}/api/v1/me/friend-requests/{{friendRequestId}}",
              "host": ["{{baseUrl}}"],
              "path": [
                "api",
                "v1",
                "me",
                "friend-requests",
                "{{friendRequestId}}"
              ]
            },
            "description": "Only the recipient may accept or reject a pending request. Acceptance notifies the original sender."
          }
        },
        {
          "name": "Cancel outgoing friend request",
          "request": {
            "method": "DELETE",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/me/friend-requests/{{outgoingFriendRequestId}}",
              "host": ["{{baseUrl}}"],
              "path": [
                "api",
                "v1",
                "me",
                "friend-requests",
                "{{outgoingFriendRequestId}}"
              ]
            },
            "description": "Only the sender can cancel a pending request."
          }
        },
        {
          "name": "List my friends",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/me/friends?limit=20&offset=0",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "me", "friends"],
              "query": [
                { "key": "limit", "value": "20" },
                { "key": "offset", "value": "0" }
              ]
            },
            "description": "Lists confirmed friends with their current level, total XP, qualifying Rosary total, and badge-instance count."
          }
        },
        {
          "name": "Get friend details and badges",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/me/friends/{{friendId}}",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "me", "friends", "{{friendId}}"]
            },
            "description": "Returns shared profile, total XP, current level, Rosary total, and every earned badge instance. Internal badge metadata is not exposed."
          }
        },
        {
          "name": "Get friends leaderboard",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/me/friends/leaderboard?periodType=weekly&limit=50&offset=0",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "me", "friends", "leaderboard"],
              "query": [
                {
                  "key": "periodType",
                  "value": "weekly",
                  "description": "daily | weekly | monthly | yearly | season"
                },
                { "key": "limit", "value": "50" },
                { "key": "offset", "value": "0" }
              ]
            },
            "description": "Ranks the authenticated user and confirmed friends by the same period points as the existing leaderboard."
          }
        },
        {
          "name": "Unfriend",
          "request": {
            "method": "DELETE",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/me/friends/{{friendId}}",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "me", "friends", "{{friendId}}"]
            },
            "description": "Removes the confirmed friendship for both users."
          }
        }
      ]
    },
    {
      "name": "Profile (me)",
      "description": "Read and update the authenticated user's profile.",
      "item": [
        {
          "name": "Get current profile",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/me",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "me"]
            },
            "description": "Returns the full profile for the authenticated user including display name, username, avatar, timezone, city, country, and visibility settings."
          }
        },
        {
          "name": "Update current profile",
          "request": {
            "method": "PATCH",
            "header": [{ "key": "Content-Type", "value": "application/json" }],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"displayName\": \"Samuel Ruiz\",\n  \"username\": \"samsoftwaredev\",\n  \"avatarUrl\": \"https://example.com/avatar.png\",\n  \"title\": \"Prayer Warrior\",\n  \"preferredLanguage\": \"en\",\n  \"timezone\": \"America/Los_Angeles\",\n  \"cityId\": null,\n  \"countryCode\": \"US\",\n  \"leaderboardVisibility\": \"public\",\n  \"prayerMapVisibility\": \"aggregated\"\n}",
              "options": { "raw": { "language": "json" } }
            },
            "url": {
              "raw": "{{baseUrl}}/api/v1/me",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "me"]
            },
            "description": "Partially updates the user's profile. Send only the fields you want to change — at least one field is required.\n\n**Fields**\n- `displayName` (string, 2–80 chars)\n- `username` (string, 3–30 chars, letters/numbers/underscores, nullable)\n- `avatarUrl` (URL string, nullable)\n- `title` (string, max 100 chars, nullable)\n- `preferredLanguage` (string, 2–10 chars, e.g. `\"en\"`)\n- `timezone` (IANA timezone string, e.g. `\"America/Los_Angeles\"`)\n- `cityId` (UUID, nullable)\n- `countryCode` (2-letter ISO code, e.g. `\"US\"`, nullable)\n- `leaderboardVisibility` (`\"public\"` | `\"friends\"` | `\"private\"`)\n- `prayerMapVisibility` (`\"aggregated\"` | `\"hidden\"`)\n\nReturns **409** if the username is already taken."
          }
        }
      ]
    },
    {
      "name": "Progress & Levels",
      "description": "XP progress, level breakdown, and the full level definition catalog.",
      "item": [
        {
          "name": "Get XP and level progress",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/progress",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "progress"]
            },
            "description": "Returns the user's total XP, lifetime/weekly/yearly points, current and next level definitions, percentage progress through the current level, and XP thresholds."
          }
        },
        {
          "name": "List active level definitions",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/levels",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "levels"]
            },
            "description": "Returns all active level definitions ordered by `level_number` ascending. Each entry contains the level name, minimum XP required, and any associated perks."
          }
        },
        {
          "name": "Get rosary completion",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/me/rosary/completion?selectedYear=2026&selectedMonth=7",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "me", "rosary", "completion"],
              "query": [
                {
                  "key": "selectedYear",
                  "value": "2026",
                  "description": "Optional UI focus year. Defaults to the authenticated user's current local year."
                },
                {
                  "key": "selectedMonth",
                  "value": "7",
                  "description": "Optional UI focus month (1–12). Defaults to the authenticated user's current local month and does not change year-to-date math."
                }
              ]
            },
            "description": "Returns the authenticated user's timezone-aware rosary completion for all twelve months of the selected year and every calendar day in the selected month. Each `days` entry contains `day`, ISO `date`, and `completed`. A local calendar day is complete when at least one self-reported or verified rosary has a `completed_at` timestamp that day; rejected rosaries are excluded.\n\nThe selected month always includes every calendar day, including dates before account creation and future dates. `completed` is `null` for a future date. Eligible-day and completed-day aggregates continue to begin on the later of January 1 and the local account-creation date, and future aggregate days are excluded. The response includes `selectedMonth` for UI focus, plus year-to-date totals that are unaffected by this parameter."
          }
        },
        {
          "name": "Get Rosary streaks",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/me/rosary/streak",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "me", "rosary", "streak"]
            },
            "description": "Returns the authenticated user's active Rosary streak and the longest streak in their current local calendar year. A local day counts when at least one self-reported or verified Rosary is completed. The active streak can end yesterday while the current local day remains incomplete; it resets after a full missed local day."
          }
        }
      ]
    },
    {
      "name": "Virtues",
      "description": "The authenticated user's current virtue values alongside the active virtue catalog.",
      "item": [
        {
          "name": "Get virtues",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/virtues",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "virtues"]
            },
            "description": "Returns every active virtue definition together with the current user's value, initialization state, and last update time. Values are read-only through this endpoint."
          }
        }
      ]
    },
    {
      "name": "Demons",
      "description": "Read-only demon catalog and individual battle definitions.",
      "item": [
        {
          "name": "List demons",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/demons",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "demons"]
            },
            "description": "Returns active demons with their saint mentor and primary/secondary virtue affinities. This is catalog data; it does not reveal another user's battle state."
          }
        },
        {
          "name": "Get demon battle definition",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/demons/{{demonCode}}",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "demons", "{{demonCode}}"]
            },
            "description": "Returns one demon's attacks, defenses, virtue affinities, saint mentor, and defeat reward. Set `demonCode`, for example `SCROLLZILLA`."
          }
        }
      ]
    },
    {
      "name": "Demon Encounters",
      "description": "Start, inspect, complete, and abandon the authenticated user's demon encounters. The server calculates all virtue, damage, XP, badge, and notification outcomes.",
      "item": [
        {
          "name": "List encounters",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/encounters?status=all&limit=20",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "encounters"],
              "query": [
                {
                  "key": "status",
                  "value": "all",
                  "description": "active | defeated | abandoned | expired | all. Default: all."
                },
                {
                  "key": "limit",
                  "value": "20",
                  "description": "Number of encounters per page. Min 1, max 100."
                },
                {
                  "key": "before",
                  "value": "",
                  "description": "Started-at cursor from `meta.nextCursor`.",
                  "disabled": true
                }
              ]
            },
            "description": "Lists the current user's encounters with demon summaries. Copy an encounter ID into `encounterId` to inspect it or perform an action."
          }
        },
        {
          "name": "Start encounter",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/json" },
              {
                "key": "Idempotency-Key",
                "value": "{{$guid}}",
                "description": "Required. Reuse this value only to retry the same start request."
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"demonCode\": \"{{demonCode}}\"\n}",
              "options": { "raw": { "language": "json" } }
            },
            "url": {
              "raw": "{{baseUrl}}/api/v1/encounters",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "encounters"]
            },
            "description": "Creates an active encounter and its first assigned defense. Save `data.encounter.id` as `encounterId` and `data.assignment.id` as `defenseAssignmentId`. A second active encounter with the same demon returns 409."
          }
        },
        {
          "name": "Get encounter detail",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/encounters/{{encounterId}}?timelineLimit=20",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "encounters", "{{encounterId}}"],
              "query": [
                {
                  "key": "timelineLimit",
                  "value": "20",
                  "description": "Battle timeline events per page. Min 1, max 100."
                },
                {
                  "key": "timelineBefore",
                  "value": "",
                  "description": "Occurred-at cursor from `meta.nextTimelineCursor`.",
                  "disabled": true
                }
              ]
            },
            "description": "Returns one owned encounter, its demon, assigned defenses, and a cursor-paginated battle timeline."
          }
        },
        {
          "name": "Complete assigned defense",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Idempotency-Key",
                "value": "{{$guid}}",
                "description": "Required. Retrying with this key returns the original authoritative battle result."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/encounters/{{encounterId}}/defenses/{{defenseAssignmentId}}/complete",
              "host": ["{{baseUrl}}"],
              "path": [
                "api",
                "v1",
                "encounters",
                "{{encounterId}}",
                "defenses",
                "{{defenseAssignmentId}}",
                "complete"
              ]
            },
            "description": "Completes an owned, assigned defense. The RPC validates state, updates the virtue and HP, records immutable events, applies a defeat reward if appropriate, and creates notifications. No client-provided reward values are accepted."
          }
        },
        {
          "name": "Abandon encounter",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Idempotency-Key",
                "value": "{{$guid}}",
                "description": "Required. Reuse this value only to retry the same abandon request."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/encounters/{{encounterId}}/abandon",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "encounters", "{{encounterId}}", "abandon"]
            },
            "description": "Ends an active owned encounter and cancels its uncompleted defense assignments. This action has no XP or virtue reward."
          }
        }
      ]
    },
    {
      "name": "Activities",
      "description": "Record and browse spiritual activities (prayer, Bible reading, fasting, etc.).",
      "item": [
        {
          "name": "List activities",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/activities?limit=20",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "activities"],
              "query": [
                {
                  "key": "limit",
                  "value": "20",
                  "description": "Number of results per page. Min 1, max 100. Default 20."
                },
                {
                  "key": "before",
                  "value": "",
                  "description": "ISO 8601 timestamp cursor. Pass `nextCursor` from a previous response to paginate.",
                  "disabled": true
                },
                {
                  "key": "activityCode",
                  "value": "",
                  "description": "Filter by activity type (e.g. PRAYER, BIBLE_READING). Case-insensitive.",
                  "disabled": true
                }
              ]
            },
            "description": "Returns the authenticated user's spiritual activities in reverse chronological order.\n\nPagination uses a cursor-based approach:\n1. Check `meta.hasMore` in the response.\n2. If `true`, pass `meta.nextCursor` as the `before` query parameter in the next request."
          }
        },
        {
          "name": "Record an activity",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/json" },
              {
                "key": "Idempotency-Key",
                "value": "{{$guid}}",
                "description": "A unique string (max 150 chars) per submission. Reusing the same key replays the original result without double-counting. Required."
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"activityCode\": \"PRAYER\",\n  \"occurredAt\": \"2026-07-18T08:00:00-07:00\",\n  \"completedAt\": \"2026-07-18T08:30:00-07:00\",\n  \"durationSeconds\": 1800,\n  \"quantity\": 1,\n  \"cityId\": null,\n  \"countryCode\": \"US\",\n  \"metadata\": {}\n}",
              "options": { "raw": { "language": "json" } }
            },
            "url": {
              "raw": "{{baseUrl}}/api/v1/activities",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "activities"]
            },
            "description": "Records a spiritual activity through a transactional RPC. The server, not the client, calculates XP, challenge updates, equipment-requirement progress, and any mapped defense completion.\n\n**Required fields**\n- `activityCode` (string, 1–50 chars) — e.g. `PRAYER`, `SCRIPTURE`, `SERVICE`, `TRUST_ACT`\n- `occurredAt` (ISO 8601 with offset) — when the activity happened\n- `quantity` (integer, 1–100) — default `1`\n\n**Optional fields**\n- `completedAt` (ISO 8601 with offset, nullable)\n- `durationSeconds` (integer, 0–86400, nullable)\n- `cityId` (UUID, nullable)\n- `countryCode` (2-letter ISO, nullable)\n- `metadata` (object) — factual context used by server-defined rules; for example `{\"planCompleted\": true}` for a completed Scripture plan or `{\"duringDifficulty\": true}` for an act of trust\n\nDo not send `virtueIncrease`, `demonDamage`, `xpAward`, `badgeEarned`, or `defenseCompleted`; those values are rejected by the request schema and calculated server-side.\n\nThe response contains `progressUpdates`, `battleUpdates`, `newBadges`, and `xp` in addition to the activity.\n\n**Idempotency-Key header** (required) — generate a UUID per submission. If you retry with the same key, the server returns `200` with the original result instead of repeating rewards."
          }
        }
      ]
    },
    {
      "name": "Challenges",
      "description": "Browse assigned challenges and claim rewards for completed ones.",
      "item": [
        {
          "name": "List challenge assignments",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/challenges?status=active",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "challenges"],
              "query": [
                {
                  "key": "status",
                  "value": "active",
                  "description": "Filter by assignment status. One of: active | completed | expired | cancelled | all. Default: active."
                }
              ]
            },
            "description": "Returns the current user's challenge assignments joined with their challenge definitions. Each item includes a computed `progressPercentage` (0–100)."
          }
        },
        {
          "name": "Claim challenge reward",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Idempotency-Key",
                "value": "{{$guid}}",
                "description": "Required. Retry the same claim with this key to return the original reward result."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/challenges/{{assignmentId}}/claim",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "challenges", "{{assignmentId}}", "claim"]
            },
            "description": "Claims the server-calculated reward for a completed challenge assignment. The assignment must be in `completed` status and may only be claimed once.\n\nSet the `assignmentId` collection variable (or path parameter) to the UUID of the assignment. The required `Idempotency-Key` prevents retrying clients from receiving duplicate XP or badges.\n\nReturns **404** if the assignment does not exist, **409** if it is incomplete or already claimed."
          }
        }
      ]
    },
    {
      "name": "Badges",
      "description": "Full badge catalog with earned badges and progress for the current user.",
      "item": [
        {
          "name": "List badges (definitions + earned + progress)",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/badges",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "badges"]
            },
            "description": "Returns all active badge definitions enriched with:\n- `earned` — array of times the user has earned this badge (may be empty)\n- `progress` — aggregate badge progress, or `null` if not started\n- `requirements` — individual unlock requirements with current/required values, completion state, and completion timestamp\n\nEquipment badges use `requirements` to show each part of an unlock. For example, Shield of Faith separately shows prayer days, completed Scripture plans, and acts of trust during difficulty."
          }
        }
      ]
    },
    {
      "name": "Leaderboards",
      "description": "Ranked leaderboard entries scoped by time period and geography.",
      "item": [
        {
          "name": "Get leaderboard entries",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/leaderboards?periodType=weekly&scopeType=global&scopeReference=global&limit=50&offset=0",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "leaderboards"],
              "query": [
                {
                  "key": "periodType",
                  "value": "weekly",
                  "description": "Time window: daily | weekly | monthly | yearly | season. Default: weekly."
                },
                {
                  "key": "periodCode",
                  "value": "",
                  "description": "Specific period code (e.g. 2026-W29). Omit to use the current active/finalized period.",
                  "disabled": true
                },
                {
                  "key": "scopeType",
                  "value": "global",
                  "description": "Geographic scope: global | country | city. Default: global."
                },
                {
                  "key": "scopeReference",
                  "value": "global",
                  "description": "Scope identifier. Use \"global\" for global scope, a 2-letter ISO code for country (e.g. \"US\"), or a city UUID."
                },
                {
                  "key": "limit",
                  "value": "50",
                  "description": "Entries per page (1–100). Default: 50."
                },
                {
                  "key": "offset",
                  "value": "0",
                  "description": "Pagination offset (0–10000). Default: 0."
                }
              ]
            },
            "description": "Returns ranked leaderboard entries for the requested period and scope. Each entry is enriched with a `profile` (display name, username, avatar, title) and an `isCurrentUser` flag.\n\nThe response also includes the matched `period` object and pagination metadata (`total`, `limit`, `offset`).\n\n**Example — country leaderboard for the US**\n`?periodType=weekly&scopeType=country&scopeReference=US`"
          }
        }
      ]
    },
    {
      "name": "Prayer Map",
      "description": "Aggregated, privacy-filtered geographic markers showing where users are praying.",
      "item": [
        {
          "name": "Get prayer map markers",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/prayer-map?level=country&from=2026-07-01T00:00:00Z&to=2026-07-31T23:59:59Z",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "prayer-map"],
              "query": [
                {
                  "key": "level",
                  "value": "country",
                  "description": "Aggregation level: country | city. Required."
                },
                {
                  "key": "from",
                  "value": "2026-07-01T00:00:00Z",
                  "description": "Start of the time window (ISO 8601). Required."
                },
                {
                  "key": "to",
                  "value": "2026-07-31T23:59:59Z",
                  "description": "End of the time window (ISO 8601). Required."
                },
                {
                  "key": "countryCode",
                  "value": "",
                  "description": "Filter to a single country (2-letter ISO code, e.g. US). Optional.",
                  "disabled": true
                }
              ]
            },
            "description": "Returns aggregated prayer map markers. Markers with fewer than **5 unique users** are suppressed to protect privacy.\n\nEach marker contains coordinates (country or city level), a `prayer_count`, and `unique_users`.\n\nResponse metadata includes the `level`, `from`, `to`, and the `privacyThreshold` (always 5)."
          }
        }
      ]
    },
    {
      "name": "Notifications",
      "description": "In-app notifications for the authenticated user.",
      "item": [
        {
          "name": "List notifications",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/notifications?limit=20&unreadOnly=false",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "notifications"],
              "query": [
                {
                  "key": "limit",
                  "value": "20",
                  "description": "Number of results (1–100). Default: 20."
                },
                {
                  "key": "unreadOnly",
                  "value": "false",
                  "description": "Pass true to return only notifications where read_at is null."
                },
                {
                  "key": "before",
                  "value": "",
                  "description": "Created-at cursor from `meta.nextCursor` for the next page.",
                  "disabled": true
                }
              ]
            },
            "description": "Returns the most recent notifications for the authenticated user, ordered newest-first, with cursor pagination metadata. Pass `unreadOnly=true` to see only unread items.\n\nBattle notification types include `demon_defense_assigned`, `demon_defense_completed`, `demon_defeated`, `demon_encounter_abandoned`, and `equipment_unlocked`. Use each notification's `action_url` and structured `payload` to navigate to the relevant encounter, defense, or badge."
          }
        },
        {
          "name": "Mark notification as read",
          "request": {
            "method": "PATCH",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/api/v1/notifications/{{notificationId}}/read",
              "host": ["{{baseUrl}}"],
              "path": [
                "api",
                "v1",
                "notifications",
                "{{notificationId}}",
                "read"
              ]
            },
            "description": "Sets `read_at` to the current timestamp for the given notification. The notification must belong to the authenticated user.\n\nSet the `notificationId` collection variable to the UUID of the notification.\n\nReturns **204 No Content** on success, **404** if the notification is not found."
          }
        }
      ]
    }
  ]
}
